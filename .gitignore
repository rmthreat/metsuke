# Build output
dist/

# Node / Cloudflare Worker tooling (test endpoint only; not part of the extension)
node_modules/
.wrangler/

# Internal docs (specs, design, store listing copy, mockups) - not shipped/tracked
docs/

# Extension signing key - NEVER commit. It controls the extension ID and
# whoever holds it can publish updates as you.
*.pem

# IDE
.idea/

# OS
.DS_Store
